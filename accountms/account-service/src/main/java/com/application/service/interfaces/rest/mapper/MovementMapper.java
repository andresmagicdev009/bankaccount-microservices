package com.application.service.interfaces.rest.mapper;

/**
 * PASO 6.2 - Frontera HTTP <-> dominio para movimientos.
 *
 * TODO: mismo patron que AccountMapper:
 *       toDomain(MovementCreateDto), toDto(Movement), toPageDto(Page<Movement>),
 *       y helpers para Double<->BigDecimal, LocalDateTime<->OffsetDateTime,
 *       String<->UUID y los enums anidados de cada DTO.
 */
public class MovementMapper {

}
